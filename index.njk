---
title: Home
layout: single.njk
---

<h1>
  {{title}}
</h1>

<p>Five recent posts</p>
<ul>
  {% for post in collections.posts %}
    {% if loop.index0 < 4 %}
      <li>
        <a href="{{post.url}}">{{ post.data.title }}</a>
      </li>
    {% endif %}
  {% endfor %}
</ul>
<p>Five recent posts</p>